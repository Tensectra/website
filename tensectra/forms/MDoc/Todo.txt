⚠️ 3 things you must do manually
1.	Create images/og-image.png — 1200×630px, dark background, your logo + tagline. Every social share currently resolves to a broken image. This is the highest-impact single task.
2.	Replace G-XXXXXXXXXX in index.html with your real Google Analytics ID — otherwise you have zero traffic data.
3.	Submit to Google Search Console at search.google.com/search-console — paste https://tensectra.com, verify ownership, then submit https://tensectra.com/sitemap.xml. Google will start indexing within 24–48 hours. Do the same on Bing Webmaster Tools.
----------------------
Before switching to public, make sure you don’t have:

API keysp;s 
Secrets (e.g. Supabase keys, Firebase config)
Passwords or tokens

👉 If you do, remove them first or move them to environment variables
----------------------
🚀 Bonus (useful for you)

Inside Netlify Forms, you can:

Export submissions (CSV)
Set email notifications
Add spam filtering
Connect to Zapier / automation
-------------
STEP 8 — SET UP EMAIL NOTIFICATIONS  (optional, 10 min)
????????????????????????????????????????????????????????
Option A — Supabase + Resend (recommended, free tier generous):
  1. Create account at resend.com
  2. Supabase ? Database ? Webhooks ? Create webhook
  3. Trigger: INSERT on contact_submissions (repeat for each table)
  4. URL: your Resend webhook endpoint
  5. Resend sends email to hello@tensectra.com on each submission

Option B — Check Supabase dashboard daily (zero setup)
  Table Editor shows all submissions with timestamps.
  Sort by submitted_at DESC to see new ones.
  -------------------
  COmplete the Crawaler Set Up now that we domain