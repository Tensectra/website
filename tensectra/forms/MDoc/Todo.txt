⚠️ 3 things you must do manually
1.	Create images/og-image.png — 1200×630px, dark background, your logo + tagline. Every social share currently resolves to a broken image. This is the highest-impact single task.
2.	Replace G-XXXXXXXXXX in index.html with your real Google Analytics ID — otherwise you have zero traffic data.
3.	Submit to Google Search Console at search.google.com/search-console — paste https://tensectra.com, verify ownership, then submit https://tensectra.com/sitemap.xml. Google will start indexing within 24–48 hours. Do the same on Bing Webmaster Tools.
----------------------
Before switching to public, make sure you don’t have:

API keysp;s 
Secrets (e.g. Supabase keys, Firebase config)
Passwords or tokens

👉 If you do, remove them first or move them to environment variables
----------------------
🚀 Bonus (useful for you)

Inside Netlify Forms, you can:

Export submissions (CSV)
Set email notifications
Add spam filtering
Connect to Zapier / automation
-------------
STEP 8 — SET UP EMAIL NOTIFICATIONS  (optional, 10 min)
????????????????????????????????????????????????????????
Option A — Supabase + Resend (recommended, free tier generous):
  1. Create account at resend.com
  2. Supabase ? Database ? Webhooks ? Create webhook
  3. Trigger: INSERT on contact_submissions (repeat for each table)
  4. URL: your Resend webhook endpoint
  5. Resend sends email to hello@tensectra.com on each submission

Option B — Check Supabase dashboard daily (zero setup)
  Table Editor shows all submissions with timestamps.
  Sort by submitted_at DESC to see new ones.
  -------------------
  COmplete the Crawaler Set Up now that we domain

  ----------------------

  WHAT YOU MUST DO MANUALLY (one-time setup)
Step	Where	What
1	Resend dashboard	Verify tensectra.com domain → get API key   
2	Vercel dashboard → Settings → Environment Variables	Add RESEND_API_KEY, PAYSTACK_SECRET_KEY, SUPABASE_SERVICE_ROLE_KEY
3	Supabase → Database → Webhooks → Create Webhook	Trigger: INSERT on consultancy_enquiries → URL: https://tensectra.com/api/notify
4	Supabase → same	Trigger: INSERT on cohort_applications → URL: https://tensectra.com/api/notify
5	Paystack dashboard → Settings → Webhooks	URL: https://tensectra.com/api/webhook/paystack


QUESTIONS TO CONFIRM BEFORE I BUILD
1.	Payment link for consultancy — amounts vary per client. Confirm: Admin enters the amount manually in the panel when sending the link? (Yes/No) -- amount is already in the  webite page -- thereis already a table in db that house the amount / service setup let the page reflect the amount based on the db table setup, and the admin can edit the amount in the admin panel if needed before sending the payment link. This way you have a default amount but can customize per client if needed.

2.	Cohort payment amount — is it fixed per cohort (e.g. Backend = $299 / ₦150,000)? Or does admin set it per student? (Recommend fixed per cohort, but can be overridden in admin panel if needed) there is db table that house the amount / service setup let the page reflect the amount based on the db table setup, and the admin can edit the amount in the admin panel if needed before sending the payment link. This way you have a default amount but can customize per student if needed.
 
3.	Resend domain — have you verified tensectra.com in Resend yet? If yes, what is your from address — hello@tensectra.com or noreply@tensectra.com? yes  hello@tensectra.com 
4.	Admin panel URL — should it be /admin (public URL, protected by login) or do you want it on a separate subdomain like admin.tensectra.com? /admin is simpler. -- (public URL, protected by login)
5.	Discord/WhatsApp links for the cohort CRM email — share those links so I can put them in the email template. -- Use a demo link forthe 2 we will replace it later when we have the actual links. For now you can use https://discord.gg/tensectra and https://wa.me/1234567890

  ----------------------
Your 5 manual steps to go live
#	Where	Action
1	Vercel → Settings → Env Vars	Add RESEND_API_KEY, PAYSTACK_SECRET_KEY, SUPABASE_SERVICE_ROLE_KEY, APP_URL=https://www.tensectra.com
2	Supabase → SQL Editor	Run Step3_AdminSchema.sql
3	Supabase → Authentication → URL Config	Set redirect URL to https://tensectra.com/admin/dashboard
4	Supabase → Database → Webhooks	INSERT on consultancy_enquiries → https://tensectra.com/api/notify; same for cohort_applications
5	Paystack → Settings → Webhooks	URL: https://tensectra.com/api/webhook/paystack


  ----------------------
What a Nigerian visitor now sees
Page	Before	After
/pages/cohorts	$299	₦150,000
/pages/cohorts (materials)	$149	₦75,000
/pages/pro	$15/month	₦7,500/month
/pages/pro	$120/year	₦60,000/year
/pages/infrastructure	$299	₦150,000


1. Go to: https://supabase.com/dashboard
2. Select: Your project (ahcfozfntvqbfgbinxwr)
3. Settings → API
4. Copy: "anon" "public" key (starts with eyJ...)
5. Update in TWO files:
   - .env (line 6)
   - js/supabase-config.js (line 5)


One thing you MUST do before email works
Go to myaccount.google.com → Security → App passwords, create a password for tensectra.office@gmail.com, then put the 16-character code (no spaces) in:
•	tcfow0ew.env → SMTP_PASSWORD=xxxxxxxxxxxx
•	Vercel dashboard → Settings → Environment Variables → SMTP_PASSWORD
That's all. Gmail regular passwords do not work for SMTP — App Password is required.


Q2 — Can Gmail send 1000 free emails?
Provider	Free limit	Best for
Resend	3,000/month, 100/day	Transactional email — better deliverability, proper SPF/DKIM
Gmail personal	500/day	Small volume, no domain setup needed
Gmail Workspace	2,000/day	If you pay for Workspace


EMAIL_PROVIDER=resend   # uses RESEND_API_KEY
EMAIL_PROVIDER=gmail    # uses SMTP_USER + SMTP_PASSWORD

  ----------------------

Fix 2 — Direct URL to self-paced tab
The self-paced section now has id="self-paced". These URLs all work:
URL	Result
http://localhost:49350/pages/courses#self-paced	Activates Self-Paced tab + scrolls to it
http://localhost:49350/pages/courses#live-cohort	Activates Live Cohort tab
http://localhost:49350/pages/courses#coming-soon	Activates Coming Soon tab
http://localhost:49350/pages/courses	Shows All (default)
How it works:



  ----------------------
What you must do right now in Vercel Dashboard
1.	Settings → Environment Variables → add SUPABASE_ANON_KEY = the anon key from supabase-config.js
2.	Deploy the changes (push to GitHub)
3.	Google Search Console → URL Inspection → paste https://tensectra.com/pages/careers → Request Indexing